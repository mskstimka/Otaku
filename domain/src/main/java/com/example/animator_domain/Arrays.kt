package com.example.animator_domain

import com.example.animator_domain.models.home.PrevPoster

val ARRAY_PREV_POSTERS = listOf(
    PrevPoster(genreId = ROMANTIC_ID, genreName = ROMANTIC_NAME),
    PrevPoster(genreId = SHOUNEN_ID, genreName = SHOUNEN_NAME),
    PrevPoster(genreId = DRAMA_ID, genreName = DRAMA_NAME),
    PrevPoster(genreId = DEMONS_ID, genreName = DEMONS_NAME),
    PrevPoster(genreId = SHOUJO_ID, genreName = SHOUJO_NAME),
    PrevPoster(genreId = HAREM_ID, genreName = HAREM_NAME)
)